fastapi==0.111.1
uvicorn[standard]==0.23.2
pydantic-settings>=2.0
pydantic>=2.0
aiokafka==0.12.0
redis==5.0.1          # modern async Redis client (replaces aioredis)
pytest==8.3.0
pytest-asyncio==0.22.0
asyncpg>=0.30.0
tenacity>=8.0          # optional, if using a retry library
SQLAlchemy>=2.0.43
psycopg2-binary>=2.9.9 # PostgreSQL driver for sync queries (optional if only asyncpg used)
structlog>=23.3.0
colorama>=0.4.6        # For colored console logs
